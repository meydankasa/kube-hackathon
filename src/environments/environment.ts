// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
    firebaseConfig: {
    apiKey: "AIzaSyCYrAnzR5wulfrI2A_DOwRmL251uimiGv0",
    authDomain: "cuve-aa9df.firebaseapp.com",
    databaseURL: "https://cuve-aa9df.firebaseio.com",
    projectId: "cuve-aa9df",
    storageBucket: "cuve-aa9df.appspot.com",
    messagingSenderId: "495010345808",
    appId: "1:495010345808:web:5c44d53cd4fca982"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
